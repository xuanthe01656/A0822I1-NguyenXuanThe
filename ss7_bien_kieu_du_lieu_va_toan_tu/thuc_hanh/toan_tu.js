function dientich(){
let inputWidth;
inputWidth = parseInt(prompt("Enter the width")) ;
let inputHeight;
inputHeight = + prompt("Enter the height");
let area = inputWidth * inputHeight;
    document.getElementById("id").innerHTML="The area is: " + area;
}